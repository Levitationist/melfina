# requirements/

The user's real, personal requirements — elicited, not assumed.

**Belongs here:** what the user actually needs the system to do, the frictions
it must remove, constraints, non-goals, and priorities. Written in the user's
terms, not in system terms.

**Does not belong here:** solutions, data models, or architecture. Those are
downstream.
