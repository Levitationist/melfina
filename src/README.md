# src/

The eventual implementation. **Placeholder only.**

Empty by intent. No code is written here until the pipeline in
`PROJECT_STATE.md` reaches CORE ENGINE and an architecture has been recorded
in `decisions/`.
